/**
 * Reports module — scaffold only for UAT.
 *
 * Post-UAT this owns: counts, conversion-rate views, office breakdown, status histograms.
 * Read-only, derived from order/tender/fleet tables — owns no tables of its own.
 */
package com.cognilogistic.reports;
