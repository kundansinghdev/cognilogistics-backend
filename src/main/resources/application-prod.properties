# Production profile — overrides insecure local/UAT defaults.
otp.delivery=twilio
otp.log-codes-in-server-logs=false

springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

management.endpoints.web.exposure.include=health,info

orders.fleet.require-vahan-consent=true
